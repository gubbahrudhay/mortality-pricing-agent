# Mortality Pricing Agent

Repository structure:
- `data/` contains datasets like `mortality_table.csv`.
- `notebooks/` contains Jupyter notebooks for pricing, sensitivity, and chatbot logic.
- `app/` contains the Streamlit application.
- `docs/` contains documentation like `final_report.docx`.
